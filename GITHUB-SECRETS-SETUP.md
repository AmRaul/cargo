# Настройка GitHub Secrets для автодеплоя

## ⚠️ Важно: Репозиторий должен быть публичным

Убедитесь, что репозиторий https://github.com/AmRaul/cargo **публичный** (Public), иначе автодеплой не сработает.

Проверить можно здесь: https://github.com/AmRaul/cargo/settings

Если репозиторий приватный - измените на Public в Settings → General → Danger Zone → Change repository visibility

---

## 🔑 Генерация паролей (на вашем компьютере)

```bash
# Генерация всех необходимых паролей
echo "SECRET_KEY=$(openssl rand -hex 32)"
echo "POSTGRES_PASSWORD=$(openssl rand -base64 24)"
echo "ADMIN_PASSWORD - придумайте сами"
```

**Скопируйте результаты!** Они понадобятся для следующего шага.

Пример вывода:
```
SECRET_KEY=f8e7d6c5b4a3928174650a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d
POSTGRES_PASSWORD=Kx9mP2nQ7vL4hR8sT6wY3zA5bC1dE0fG
```

## 📝 Добавление Secrets в GitHub

1. Откройте ваш репозиторий: https://github.com/AmRaul/cargo
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** для каждого секрета:

### Обязательные Secrets:

| Name | Value | Описание |
|------|-------|----------|
| `SERVER_HOST` | `5.35.80.213` | IP адрес сервера |
| `SERVER_USER` | `root` | Пользователь SSH |
| `SSH_PRIVATE_KEY` | `-----BEGIN OPENSSH PRIVATE KEY-----...` | Приватный SSH ключ |
| `POSTGRES_PASSWORD` | Из команды `openssl rand -base64 24` | Пароль базы данных |
| `SECRET_KEY` | Из команды `openssl rand -hex 32` | Секретный ключ FastAPI |
| `ADMIN_PASSWORD` | Придумайте сами | Пароль админ-панели |

## 🔐 Генерация SSH ключа (если еще не создан)

```bash
# Генерация нового SSH ключа
ssh-keygen -t ed25519 -C "github-actions-cargo" -f ~/.ssh/github_cargo

# Копирование публичного ключа на сервер
ssh-copy-id -i ~/.ssh/github_cargo.pub root@5.35.80.213

# Показать приватный ключ для GitHub
cat ~/.ssh/github_cargo
```

Скопируйте **весь вывод** (от `-----BEGIN` до `-----END`) и вставьте в `SSH_PRIVATE_KEY`.

## ✅ Проверка настройки

После добавления всех секретов:

1. **На сервере** установите Docker и Git:
```bash
ssh root@5.35.80.213
curl -fsSL https://get.docker.com | sh
apt install docker-compose git -y
exit
```

2. **Запушьте код в GitHub**:
```bash
git add .
git commit -m "Add deployment configuration"
git push origin main
```

3. **GitHub Actions автоматически**:
   - Склонирует репозиторий в `/opt/cargo` (если еще не существует)
   - Создаст `.env` файл на сервере из секретов
   - Запустит деплой

4. **Проверьте статус**: https://github.com/AmRaul/cargo/actions

## 📋 Список всех Secrets

Всего нужно добавить **6 секретов**:

- ✅ `SERVER_HOST`
- ✅ `SERVER_USER`
- ✅ `SSH_PRIVATE_KEY`
- ✅ `POSTGRES_PASSWORD`
- ✅ `SECRET_KEY`
- ✅ `ADMIN_PASSWORD`

## 🚀 Первый деплой

После настройки секретов и push в GitHub:

```bash
# 1. GitHub Actions автоматически запустится и задеплоит проект
# 2. Подключитесь к серверу для настройки SSL
ssh root@5.35.80.213
cd /opt/cargo

# Настройте SSL (только первый раз!)
nano init-ssl.sh  # Измените EMAIL на ваш email
chmod +x init-ssl.sh
./init-ssl.sh
```

**Важно**: SSL нужно настроить только один раз после первого деплоя!

После этого **все деплои полностью автоматические** - просто `git push origin main`!

## 🔄 Как это работает

1. Вы делаете `git push origin main`
2. GitHub Actions:
   - Подключается к серверу по SSH
   - Создает `.env` файл из секретов
   - Выполняет `git pull`
   - Запускает `deploy.sh`
3. Сайт автоматически обновляется!

## ⚠️ Важно

- **Никогда** не коммитьте `.env` файл в Git
- Пароли храните только в GitHub Secrets
- Меняйте `ADMIN_PASSWORD` на надежный пароль
- SSH ключ должен быть уникальным для этого проекта

## 🆘 Если что-то пошло не так

Проверьте логи GitHub Actions:
https://github.com/AmRaul/cargo/actions

Или проверьте на сервере:
```bash
ssh root@5.35.80.213
cd /opt/cargo
cat .env  # Проверьте, что файл создан
docker-compose -f docker-compose.prod.yml logs
```
