# 🐳 Cargo Logistics - Docker Quick Start

Полная инструкция по запуску системы через Docker.

## 🚀 Быстрый старт

### Вариант 1: Автоматический (одна команда)

```bash
./START.sh
```

Скрипт сам:
- ✅ Создаст `.env` из шаблона
- ✅ Соберёт и запустит контейнеры
- ✅ Применит миграции
- ✅ Создаст тестовые данные

### Вариант 2: Ручной (3 шага)

```bash
# 1. Создать .env файл
cp .env.example .env

# 2. Запустить контейнеры
docker-compose up --build -d

# 3. Создать тестовые данные (опционально)
docker-compose exec backend python create_initial_data.py
```

### 🔐 Доступ

- **Admin**: http://localhost:8000/admin
- **Логин**: `admin`
- **Пароль**: смотри в `.env` (по умолчанию `admin123`)
- **API Docs**: http://localhost:8000/docs

Готово! 🎉

---

## 📋 Подробная инструкция

### Шаг 0: Создание .env файла

⚠️ **Обязательно!** Перед первым запуском:

```bash
# Скопировать шаблон
cp .env.example .env

# (Опционально) Изменить пароли для production
nano .env
```

Файл `.env` содержит:
- `DATABASE_URL` - строка подключения к БД
- `SECRET_KEY` - секретный ключ для сессий
- `ADMIN_PASSWORD` - пароль админки
- `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` - настройки PostgreSQL

### Шаг 1: Запуск контейнеров

```bash
docker-compose up --build -d
```

Что происходит:
- ✅ Docker Compose читает переменные из `.env` файла
- ✅ Создаётся PostgreSQL контейнер с настройками из `.env`
- ✅ Собирается backend контейнер
- ✅ Автоматически применяются Alembic миграции
- ✅ Запускается FastAPI сервер на порту 8000

### Шаг 2: Проверка статуса

```bash
# Посмотреть логи
docker-compose logs -f backend

# Проверить что все контейнеры запущены
docker-compose ps
```

Должны быть запущены:
- `cargo-postgres` - база данных
- `cargo-backend` - FastAPI сервер

### Шаг 3: Создание тестовых данных (опционально)

```bash
docker-compose exec backend python create_initial_data.py
```

Создаст:
- 2 поставщика (Китай)
- 2 клиента (РФ)
- 3 ставки
- 3 поставки
- 4 расхода

---

## 🌐 Доступные URL

| Сервис | URL | Описание |
|--------|-----|----------|
| **Admin Panel** | http://localhost:8000/admin | Админка (login: `admin` / pass: `admin123`) |
| **API Docs** | http://localhost:8000/docs | Swagger документация |
| **API Root** | http://localhost:8000 | API информация |
| **Health Check** | http://localhost:8000/health | Проверка работоспособности |

---

## 🔧 Полезные команды

### Управление контейнерами

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Остановить и удалить volumes (очистить БД)
docker-compose down -v
```

### Работа с базой данных

```bash
# Подключиться к PostgreSQL
docker-compose exec postgres psql -U cargo_user -d cargo_db

# Посмотреть таблицы
docker-compose exec postgres psql -U cargo_user -d cargo_db -c "\dt"

# Backup базы данных
docker-compose exec postgres pg_dump -U cargo_user cargo_db > backup.sql

# Restore базы данных
cat backup.sql | docker-compose exec -T postgres psql -U cargo_user -d cargo_db
```

### Работа с миграциями

```bash
# Применить миграции вручную
docker-compose exec backend alembic upgrade head

# Посмотреть текущую версию
docker-compose exec backend alembic current

# Создать новую миграцию
docker-compose exec backend alembic revision --autogenerate -m "описание"

# Откатить миграцию
docker-compose exec backend alembic downgrade -1
```

### Логи и отладка

```bash
# Смотреть логи в реальном времени
docker-compose logs -f backend

# Смотреть только последние 100 строк
docker-compose logs --tail=100 backend

# Зайти в контейнер backend
docker-compose exec backend bash

# Зайти в контейнер postgres
docker-compose exec postgres bash
```

### Создание данных

```bash
# Создать тестовые данные
docker-compose exec backend python create_initial_data.py

# Запустить Python shell в контейнере
docker-compose exec backend python
```

---

## 🔐 Настройки безопасности

### Изменить пароли (ВАЖНО для продакшена!)

⚠️ **НЕ редактируйте `docker-compose.yml`!** Все пароли в `.env` файле.

1. **Откройте `.env` файл**:
   ```bash
   nano .env  # или любой редактор
   ```

2. **Измените пароли**:
   ```env
   # Пароль админки
   ADMIN_PASSWORD=ваш-сложный-пароль

   # Секретный ключ для сессий (сгенерируйте!)
   SECRET_KEY=ваш-секретный-ключ-32-символа

   # Пароль PostgreSQL
   POSTGRES_PASSWORD=пароль-для-бд

   # Не забудьте обновить DATABASE_URL!
   DATABASE_URL=postgresql://cargo_user:пароль-для-бд@postgres:5432/cargo_db
   ```

3. **Сгенерируйте безопасный SECRET_KEY**:
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

4. **Перезапустите контейнеры**:
   ```bash
   docker-compose down
   docker-compose up -d
   ```

### ⚠️ Критически важно:

- ✅ `.env` файл уже в `.gitignore` - не попадёт в git
- ✅ Используйте разные пароли для dev и production
- ✅ Никогда не коммитьте `.env` в репозиторий
- ✅ Для production используйте сложные пароли (20+ символов)

---

## 📊 Примеры использования API

### Через curl

```bash
# Получить список поставщиков
curl http://localhost:8000/api/v1/suppliers

# Создать клиента
curl -X POST http://localhost:8000/api/v1/clients \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test Client",
    "company_name": "Test LLC",
    "contact_info": "+7-999-999-9999"
  }'

# Получить финансы по поставке
curl http://localhost:8000/api/v1/shipments/{shipment_id}/finance

# Получить сводный отчёт
curl "http://localhost:8000/api/v1/reports/summary?date_from=2024-01-01&date_to=2024-12-31"
```

### Через Python

```python
import requests

# Получить все поставки
response = requests.get("http://localhost:8000/api/v1/shipments")
shipments = response.json()

# Создать поставщика
supplier_data = {
    "name": "Beijing Trading",
    "country": "China",
    "city": "Beijing"
}
response = requests.post(
    "http://localhost:8000/api/v1/suppliers",
    json=supplier_data
)
supplier = response.json()
print(f"Created supplier: {supplier['id']}")
```

---

## 🐛 Troubleshooting

### Контейнер backend падает при старте

```bash
# Посмотреть логи
docker-compose logs backend

# Часто помогает пересборка
docker-compose down
docker-compose up --build
```

### Ошибка подключения к БД

```bash
# Проверить что postgres запущен
docker-compose ps

# Проверить healthcheck
docker inspect cargo-postgres | grep Health

# Перезапустить PostgreSQL
docker-compose restart postgres
```

### Нужно очистить БД и начать заново

```bash
# Остановить и удалить всё (включая volumes)
docker-compose down -v

# Запустить заново
docker-compose up --build -d

# Создать тестовые данные
docker-compose exec backend python create_initial_data.py
```

### Порт 8000 или 5432 уже занят

Отредактируйте `docker-compose.yml` и измените маппинг портов:

```yaml
ports:
  - "8001:8000"  # backend (было 8000:8000)
  - "5433:5432"  # postgres (было 5432:5432)
```

---

## 🚢 Production Deployment

Для продакшена используйте отдельный файл:

```bash
docker-compose -f docker-compose.prod.yml up -d
```

Обязательно:
1. ✅ Измените все пароли и секреты
2. ✅ Используйте внешний PostgreSQL (не в контейнере)
3. ✅ Настройте SSL/TLS
4. ✅ Настройте backup базы данных
5. ✅ Ограничьте CORS в production

---

## 📝 Структура проекта

```
cargo/
├── docker-compose.yml          # Конфигурация Docker для dev
├── backend/
│   ├── Dockerfile              # Образ для backend
│   ├── entrypoint.sh           # Скрипт запуска (миграции + server)
│   ├── requirements.txt        # Python зависимости
│   ├── alembic.ini            # Конфигурация Alembic
│   ├── alembic/
│   │   └── versions/          # Миграции БД
│   ├── app/
│   │   ├── main.py            # Точка входа FastAPI
│   │   ├── models/            # SQLAlchemy модели
│   │   ├── schemas/           # Pydantic схемы
│   │   ├── api/               # API endpoints
│   │   ├── admin/             # SQLAdmin views
│   │   └── services/          # Бизнес-логика
│   └── create_initial_data.py # Скрипт для тестовых данных
└── ...
```

---

## ✅ Checklist для деплоя

- [ ] Изменены все пароли в `docker-compose.yml`
- [ ] Настроен SECRET_KEY (случайная строка 32+ символа)
- [ ] База данных создана и доступна
- [ ] Применены миграции: `alembic upgrade head`
- [ ] Сервер доступен по URL
- [ ] Admin panel работает
- [ ] API endpoints отвечают
- [ ] Настроен backup базы данных

---

## 🆘 Поддержка

Проблемы? Создайте issue в репозитории или проверьте логи:

```bash
docker-compose logs -f
```
