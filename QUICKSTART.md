# 🚀 Быстрый старт

## Запуск проекта (3 простых шага)

### 1. Клонировать репозиторий
```bash
git clone <repository-url>
cd cargo
```

### 2. Запустить проект

**Linux/Mac:**
```bash
./start.sh
```

**Windows:**
```bash
start.bat
```

**Или вручную:**
```bash
docker-compose up --build
```

### 3. Открыть в браузере

- 🌐 **Лендинг**: http://localhost:3000
- 📡 **API**: http://localhost:8000
- 📚 **Документация API**: http://localhost:8000/docs

## ✅ Готово!

Теперь вы можете:
- Заполнить форму заявки на лендинге
- Посмотреть API документацию
- Тестировать API через Swagger UI

## 🛠️ Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend

# Остановить проект
docker-compose down

# Остановить и удалить данные
docker-compose down -v

# Пересобрать без кеша
docker-compose build --no-cache
```

## 📊 Проверка работы

### Тест создания заявки через curl:

```bash
curl -X POST "http://localhost:8000/api/v1/orders/" \
  -H "Content-Type: application/json" \
  -d '{
    "client_name": "Тест Тестов",
    "client_phone": "+79991234567",
    "client_email": "test@example.com",
    "route": "uae_to_rf",
    "cargo_type": "Электроника",
    "cargo_weight": 50.5,
    "description": "Тестовая заявка"
  }'
```

### Получить список заявок:

```bash
curl "http://localhost:8000/api/v1/orders/"
```

## 🐛 Проблемы?

1. **Порт уже занят?**
   - Измените порты в `docker-compose.yml`

2. **Docker не запускается?**
   - Убедитесь что Docker Desktop запущен

3. **Ошибка сборки frontend?**
   - Попробуйте: `docker-compose build --no-cache frontend`

4. **База данных не подключается?**
   - Проверьте логи: `docker-compose logs postgres`

## 📝 Следующие шаги

1. Настроить админ панель (см. README.md)
2. Добавить email/SMS уведомления
3. Настроить домен и SSL
4. Добавить мониторинг

Подробнее в [README.md](./README.md)
